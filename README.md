# Netshell - Networked Shell

Based on my dissertation project idea.

## Build
```
git clone https://github.com/kk1201/netshell
cd netshell
mkdir build && cd build
cmake ../ -DCMAKE_EXPORT_COMPILE_COMMANDS=ON -DCMAKE_BUILD_TYPE=Debug
cmake --build .
```
